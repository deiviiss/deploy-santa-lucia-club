
//interface Payment type data Membership Ids
export interface IPaymentTypeIds {
    idMembership: string;
    idPlanProvider: string;
}

//interface Payment type data Membership Info
export interface IPaymentTypeInfo {
    name: string;
    price: number;
    paymentFrequency: string;
}